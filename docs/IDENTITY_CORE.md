# Identity Core (Work)

Work trusts **Nexus** as the auth authority when `VITE_AUTH_SUPABASE_*` is set.

Full ops guide (JWT secret alignment, allowlist, dogfood checklist):  
in the Nexus repo: `docs/IDENTITY_CORE.md`.

Module verify / API keys are unchanged.
